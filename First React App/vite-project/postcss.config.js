export default {
  plugins: {
    "postcss-import": {}, // Optional, only if you're using imports
    "@tailwindcss/postcss": {}, // Replace 'tailwindcss' with this
    autoprefixer: {}, // Optional, but recommended
  },
};